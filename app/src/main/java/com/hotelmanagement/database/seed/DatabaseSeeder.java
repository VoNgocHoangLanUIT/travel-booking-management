package com.hotelmanagement.database.seed;

import com.hotelmanagement.database.AppDatabase;

public class DatabaseSeeder {
    // Seed data removed; seeding is disabled after one-time migration.

    public static void resetAndSeed(AppDatabase database) {
        // no-op
    }

    public static void seedIfEmpty(AppDatabase database) {
        // no-op
    }
}
